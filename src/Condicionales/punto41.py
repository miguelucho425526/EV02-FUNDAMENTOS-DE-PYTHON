numeros = [0, 0, 1, 0]

if any(numeros):
    print("Al menos un número es no cero.")

"""En este ejemplo se utiliza la función 'any()' para verificar si hay al menos un elemento no cero en la lista 'numeros'."""